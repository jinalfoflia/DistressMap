{
  "name": "distress-map",
  "version": "0.0.1",
  "description": "A map to allow anyone to share their story of abuse/violence/incidents anonymously",
  "private": true,
  "scripts": {
    "start": "npm run watch & serve .",
    "watch": "watchify index.js -o build.js -v",
    "build": "browserify index.js -o build.js -v"
  },
  "author": "Jinal Foflia",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^13.3.0",
    "serve": "^2.4.2",
    "watchify": "^3.8.0"
  }
}
